{
  "name": "ig_hd_video_downloader",
  "version": "2.0.0",
  "description": "Minimal Instagram HD video downloader with FFmpeg support",
  "scripts": {
    "build": "node build-native.js",
    "build:production": "npm run build && node zip-dist.js",
    "build:watch": "npx tsc --watch",
    "lint": "eslint src/**/*.ts",
    "lint:fix": "eslint src/**/*.ts --fix"
  },
  "dependencies": {
    "@ffmpeg/core": "^0.12.6",
    "@ffmpeg/ffmpeg": "^0.12.15",
    "@ffmpeg/util": "^0.12.2",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@types/webextension-polyfill": "^0.12.0",
    "@typescript-eslint/eslint-plugin": "^8.18.1",
    "@typescript-eslint/parser": "^8.18.1",
    "archiver": "^7.0.1",
    "eslint": "^9.16.0",
    "sass": "^1.81.1",
    "typescript": "^5.7.0"
  }
}